/**
 * Placeholder API key passed to upstream OpenAI-compatible clients when auth is
 * provided by the gateway-aware fetch layer instead of the upstream provider.
 */
export const GATEWAY_PLACEHOLDER_API_KEY = 'ai-gateway-auth-via-fetch';
